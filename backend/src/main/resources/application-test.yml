spring:
  datasource:
    url: jdbc:h2:mem:ford_test;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: false

  flyway:
    enabled: false

jwt:
  secret: segredo-de-teste-com-pelo-menos-64-caracteres-obrigatorio-para-hs256
  expiration-ms: 3600000
